const initState = {
  open : false ,
  connect : false ,
};

const stateReducer = ( state = initState, action) => {
    switch (action.type){
        default:
            return state;
    }
};

export default stateReducer ;